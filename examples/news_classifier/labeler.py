# from labelai import Labeler, Dataset, Taxonomy, Embedder, Model


# CONFIG = "config.yaml"

# dataset = Dataset()
# embedder = Embedder()
# taxonomy = Taxonomy()


# model_ensemble = [
#     Model(name='gpt-4'),
#     Model(name='claude-sonnet'),
#     Model(name='qwen'),
#     Model(name='gemini-2.5'),
#     Model(name='grok')
# ]

# labeler = Labeler(config=CONFIG, taxonomy=taxonomy, dataset=dataset, model=model_ensemble, voter=voter)
# labeler.label()

